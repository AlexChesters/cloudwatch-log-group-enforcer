[project]
name = "cloudwatch-log-group-enforcer"
version = "0.1.0"
description = ""
authors = [{ name = "Alex Chesters", email = "10558792+AlexChesters@users.noreply.github.com" }]
requires-python = ">=3.14,<4"
readme = "README.md"
license = "MIT"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14"
]
dependencies = ["boto3>=1.42.81,<2"]

[tool.uv]

[tool.uv.build-backend]
module-name = ["cloudwatch_log_group_enforcer"]
module-root = ""

[build-system]
requires = ["uv_build>=0.11.0,<0.12.0"]
build-backend = "uv_build"
